# secrets never in git — they live in /data/docker/gyrus/ at 600
.env
*.env
secrets/
*.key
*.pem

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# local scratch / consolidation reports
data/
dream-reports/
*.log

# golden-set windows contain raw private conversation content — never commit
tools/extraction-eval/goldens/
# session-to-session notes may contain raw private content — keep local
docs/shadesmar-notes/*
!docs/shadesmar-notes/.gitkeep
